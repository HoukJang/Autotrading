--- a/autotrading/data/continuous_collector.py
+++ b/autotrading/data/continuous_collector.py
@@ -326,9 +326,14 @@ class GapFiller:
         try:
             logger.info(f"Collecting initial 1 month data for {symbol}")
 
-            # 1개월 데이터 요청
+            # 현재 활성 계약 사용
+            from datetime import datetime
+            utils = FuturesContractUtils()
+            
+            # 1개월 데이터 요청 (target_date 전달하여 contract_month 자동 설정)
             bars = await self.fetcher.fetch_historical_bars(
                 symbol=symbol,
                 duration='1 M',
-                bar_size='1 min'
+                bar_size='1 min',
+                target_date=datetime.now()
             )
